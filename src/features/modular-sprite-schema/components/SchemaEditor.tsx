import { SemanticCatalog } from '@kukla2d/modular-sprite-schema';

import type { NewSchemaMetadata } from '../application/schemaMetadata.js';

const catalog=new SemanticCatalog();
export function SchemaEditor({ enabled, onEnabledChange, value, onChange, existingApplied, canRevise, saveMode, onSaveModeChange }:{enabled:boolean;onEnabledChange:(value:boolean)=>void;value:NewSchemaMetadata;onChange:(value:NewSchemaMetadata)=>void;existingApplied?:boolean;canRevise?:boolean;saveMode:'new'|'revision';onSaveModeChange:(mode:'new'|'revision')=>void}):React.ReactElement { const types=catalog.list('character-type');const classes=catalog.list('character-class');return <section className="space-y-3 rounded-lg border p-4"><label className="flex items-center gap-3 text-sm"><input type="checkbox" checked={enabled} onChange={event=>onEnabledChange(event.target.checked)}/>Add this schema to database</label>{enabled&&<div className="grid gap-3 border-t pt-3">{existingApplied&&<label className="grid gap-1 text-xs">Save strategy<select className="h-9 rounded-md border bg-background px-2" value={saveMode} onChange={event=>onSaveModeChange(event.target.value as 'new'|'revision')}><option value="new">Save as new schema</option>{canRevise&&<option value="revision">Save as new revision</option>}</select></label>}<p className="text-xs text-muted-foreground">{saveMode==='revision'?'The edited layout will become the next revision of your schema.':'A reusable fingerprint and semantic layout will be saved locally.'}</p><label className="grid gap-1 text-xs">Schema name<input className="h-9 rounded-md border bg-background px-2" value={value.name} onChange={event=>onChange({...value,name:event.target.value})}/></label><label className="grid gap-1 text-xs">Description<textarea className="min-h-16 rounded-md border bg-background p-2" value={value.description} onChange={event=>onChange({...value,description:event.target.value})}/></label><label className="grid gap-1 text-xs">Character type<select className="h-9 rounded-md border bg-background px-2" value={value.characterTypeIds[0]??''} onChange={event=>onChange({...value,characterTypeIds:event.target.value?[event.target.value]:[]})}><option value="">Unspecified</option>{types.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select></label><label className="grid gap-1 text-xs">Character class<select className="h-9 rounded-md border bg-background px-2" value={value.characterClassIds[0]??''} onChange={event=>onChange({...value,characterClassIds:event.target.value?[event.target.value]:[]})}><option value="">Unspecified</option>{classes.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select></label><label className="grid gap-1 text-xs">Tags (comma separated)<input className="h-9 rounded-md border bg-background px-2" value={value.tags.join(', ')} onChange={event=>onChange({...value,tags:event.target.value.split(',').map(item=>item.trim()).filter(Boolean)})}/></label></div>}</section>; }
